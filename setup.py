from setuptools import setup

if __name__ == "__main__":
    setup(
        name = 'utils',
        version = '0.0.1',
        url = 'https://github.com/shimonport/utils.git',
        author = 'Shimon Port',
        packages=['utils']
    )